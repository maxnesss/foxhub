import { RevealWithDelayDirective } from './reveal-with-delay.directive';

describe('RevealWithDelayDirective', () => {
  it('should create an instance', () => {
    const directive = new RevealWithDelayDirective();
    expect(directive).toBeTruthy();
  });
});
